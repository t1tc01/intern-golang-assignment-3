// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.com/hedwig-phan/assignment-3/ent/earthquake"
	"gitlab.com/hedwig-phan/assignment-3/ent/featuretype"
	"gitlab.com/hedwig-phan/assignment-3/ent/ftypeearthquake"
	"gitlab.com/hedwig-phan/assignment-3/ent/predicate"
)

// FtypeEarthquakeQuery is the builder for querying FtypeEarthquake entities.
type FtypeEarthquakeQuery struct {
	config
	ctx             *QueryContext
	order           []ftypeearthquake.OrderOption
	inters          []Interceptor
	predicates      []predicate.FtypeEarthquake
	withEarthquake  *EarthquakeQuery
	withFeatureType *FeatureTypeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FtypeEarthquakeQuery builder.
func (feq *FtypeEarthquakeQuery) Where(ps ...predicate.FtypeEarthquake) *FtypeEarthquakeQuery {
	feq.predicates = append(feq.predicates, ps...)
	return feq
}

// Limit the number of records to be returned by this query.
func (feq *FtypeEarthquakeQuery) Limit(limit int) *FtypeEarthquakeQuery {
	feq.ctx.Limit = &limit
	return feq
}

// Offset to start from.
func (feq *FtypeEarthquakeQuery) Offset(offset int) *FtypeEarthquakeQuery {
	feq.ctx.Offset = &offset
	return feq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (feq *FtypeEarthquakeQuery) Unique(unique bool) *FtypeEarthquakeQuery {
	feq.ctx.Unique = &unique
	return feq
}

// Order specifies how the records should be ordered.
func (feq *FtypeEarthquakeQuery) Order(o ...ftypeearthquake.OrderOption) *FtypeEarthquakeQuery {
	feq.order = append(feq.order, o...)
	return feq
}

// QueryEarthquake chains the current query on the "earthquake" edge.
func (feq *FtypeEarthquakeQuery) QueryEarthquake() *EarthquakeQuery {
	query := (&EarthquakeClient{config: feq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := feq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := feq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ftypeearthquake.Table, ftypeearthquake.FieldID, selector),
			sqlgraph.To(earthquake.Table, earthquake.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, ftypeearthquake.EarthquakeTable, ftypeearthquake.EarthquakeColumn),
		)
		fromU = sqlgraph.SetNeighbors(feq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFeatureType chains the current query on the "feature_type" edge.
func (feq *FtypeEarthquakeQuery) QueryFeatureType() *FeatureTypeQuery {
	query := (&FeatureTypeClient{config: feq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := feq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := feq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ftypeearthquake.Table, ftypeearthquake.FieldID, selector),
			sqlgraph.To(featuretype.Table, featuretype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, ftypeearthquake.FeatureTypeTable, ftypeearthquake.FeatureTypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(feq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FtypeEarthquake entity from the query.
// Returns a *NotFoundError when no FtypeEarthquake was found.
func (feq *FtypeEarthquakeQuery) First(ctx context.Context) (*FtypeEarthquake, error) {
	nodes, err := feq.Limit(1).All(setContextOp(ctx, feq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ftypeearthquake.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) FirstX(ctx context.Context) *FtypeEarthquake {
	node, err := feq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FtypeEarthquake ID from the query.
// Returns a *NotFoundError when no FtypeEarthquake ID was found.
func (feq *FtypeEarthquakeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = feq.Limit(1).IDs(setContextOp(ctx, feq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ftypeearthquake.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) FirstIDX(ctx context.Context) int {
	id, err := feq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FtypeEarthquake entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FtypeEarthquake entity is found.
// Returns a *NotFoundError when no FtypeEarthquake entities are found.
func (feq *FtypeEarthquakeQuery) Only(ctx context.Context) (*FtypeEarthquake, error) {
	nodes, err := feq.Limit(2).All(setContextOp(ctx, feq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ftypeearthquake.Label}
	default:
		return nil, &NotSingularError{ftypeearthquake.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) OnlyX(ctx context.Context) *FtypeEarthquake {
	node, err := feq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FtypeEarthquake ID in the query.
// Returns a *NotSingularError when more than one FtypeEarthquake ID is found.
// Returns a *NotFoundError when no entities are found.
func (feq *FtypeEarthquakeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = feq.Limit(2).IDs(setContextOp(ctx, feq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ftypeearthquake.Label}
	default:
		err = &NotSingularError{ftypeearthquake.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) OnlyIDX(ctx context.Context) int {
	id, err := feq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FtypeEarthquakes.
func (feq *FtypeEarthquakeQuery) All(ctx context.Context) ([]*FtypeEarthquake, error) {
	ctx = setContextOp(ctx, feq.ctx, "All")
	if err := feq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FtypeEarthquake, *FtypeEarthquakeQuery]()
	return withInterceptors[[]*FtypeEarthquake](ctx, feq, qr, feq.inters)
}

// AllX is like All, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) AllX(ctx context.Context) []*FtypeEarthquake {
	nodes, err := feq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FtypeEarthquake IDs.
func (feq *FtypeEarthquakeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if feq.ctx.Unique == nil && feq.path != nil {
		feq.Unique(true)
	}
	ctx = setContextOp(ctx, feq.ctx, "IDs")
	if err = feq.Select(ftypeearthquake.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) IDsX(ctx context.Context) []int {
	ids, err := feq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (feq *FtypeEarthquakeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, feq.ctx, "Count")
	if err := feq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, feq, querierCount[*FtypeEarthquakeQuery](), feq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) CountX(ctx context.Context) int {
	count, err := feq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (feq *FtypeEarthquakeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, feq.ctx, "Exist")
	switch _, err := feq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (feq *FtypeEarthquakeQuery) ExistX(ctx context.Context) bool {
	exist, err := feq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FtypeEarthquakeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (feq *FtypeEarthquakeQuery) Clone() *FtypeEarthquakeQuery {
	if feq == nil {
		return nil
	}
	return &FtypeEarthquakeQuery{
		config:          feq.config,
		ctx:             feq.ctx.Clone(),
		order:           append([]ftypeearthquake.OrderOption{}, feq.order...),
		inters:          append([]Interceptor{}, feq.inters...),
		predicates:      append([]predicate.FtypeEarthquake{}, feq.predicates...),
		withEarthquake:  feq.withEarthquake.Clone(),
		withFeatureType: feq.withFeatureType.Clone(),
		// clone intermediate query.
		sql:  feq.sql.Clone(),
		path: feq.path,
	}
}

// WithEarthquake tells the query-builder to eager-load the nodes that are connected to
// the "earthquake" edge. The optional arguments are used to configure the query builder of the edge.
func (feq *FtypeEarthquakeQuery) WithEarthquake(opts ...func(*EarthquakeQuery)) *FtypeEarthquakeQuery {
	query := (&EarthquakeClient{config: feq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	feq.withEarthquake = query
	return feq
}

// WithFeatureType tells the query-builder to eager-load the nodes that are connected to
// the "feature_type" edge. The optional arguments are used to configure the query builder of the edge.
func (feq *FtypeEarthquakeQuery) WithFeatureType(opts ...func(*FeatureTypeQuery)) *FtypeEarthquakeQuery {
	query := (&FeatureTypeClient{config: feq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	feq.withFeatureType = query
	return feq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FtID int `json:"ft_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FtypeEarthquake.Query().
//		GroupBy(ftypeearthquake.FieldFtID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (feq *FtypeEarthquakeQuery) GroupBy(field string, fields ...string) *FtypeEarthquakeGroupBy {
	feq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FtypeEarthquakeGroupBy{build: feq}
	grbuild.flds = &feq.ctx.Fields
	grbuild.label = ftypeearthquake.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FtID int `json:"ft_id,omitempty"`
//	}
//
//	client.FtypeEarthquake.Query().
//		Select(ftypeearthquake.FieldFtID).
//		Scan(ctx, &v)
func (feq *FtypeEarthquakeQuery) Select(fields ...string) *FtypeEarthquakeSelect {
	feq.ctx.Fields = append(feq.ctx.Fields, fields...)
	sbuild := &FtypeEarthquakeSelect{FtypeEarthquakeQuery: feq}
	sbuild.label = ftypeearthquake.Label
	sbuild.flds, sbuild.scan = &feq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FtypeEarthquakeSelect configured with the given aggregations.
func (feq *FtypeEarthquakeQuery) Aggregate(fns ...AggregateFunc) *FtypeEarthquakeSelect {
	return feq.Select().Aggregate(fns...)
}

func (feq *FtypeEarthquakeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range feq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, feq); err != nil {
				return err
			}
		}
	}
	for _, f := range feq.ctx.Fields {
		if !ftypeearthquake.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if feq.path != nil {
		prev, err := feq.path(ctx)
		if err != nil {
			return err
		}
		feq.sql = prev
	}
	return nil
}

func (feq *FtypeEarthquakeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FtypeEarthquake, error) {
	var (
		nodes       = []*FtypeEarthquake{}
		_spec       = feq.querySpec()
		loadedTypes = [2]bool{
			feq.withEarthquake != nil,
			feq.withFeatureType != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FtypeEarthquake).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FtypeEarthquake{config: feq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, feq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := feq.withEarthquake; query != nil {
		if err := feq.loadEarthquake(ctx, query, nodes, nil,
			func(n *FtypeEarthquake, e *Earthquake) { n.Edges.Earthquake = e }); err != nil {
			return nil, err
		}
	}
	if query := feq.withFeatureType; query != nil {
		if err := feq.loadFeatureType(ctx, query, nodes, nil,
			func(n *FtypeEarthquake, e *FeatureType) { n.Edges.FeatureType = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (feq *FtypeEarthquakeQuery) loadEarthquake(ctx context.Context, query *EarthquakeQuery, nodes []*FtypeEarthquake, init func(*FtypeEarthquake), assign func(*FtypeEarthquake, *Earthquake)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FtypeEarthquake)
	for i := range nodes {
		fk := nodes[i].EqID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(earthquake.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "eq_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (feq *FtypeEarthquakeQuery) loadFeatureType(ctx context.Context, query *FeatureTypeQuery, nodes []*FtypeEarthquake, init func(*FtypeEarthquake), assign func(*FtypeEarthquake, *FeatureType)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FtypeEarthquake)
	for i := range nodes {
		fk := nodes[i].FtID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(featuretype.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "ft_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (feq *FtypeEarthquakeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := feq.querySpec()
	_spec.Node.Columns = feq.ctx.Fields
	if len(feq.ctx.Fields) > 0 {
		_spec.Unique = feq.ctx.Unique != nil && *feq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, feq.driver, _spec)
}

func (feq *FtypeEarthquakeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ftypeearthquake.Table, ftypeearthquake.Columns, sqlgraph.NewFieldSpec(ftypeearthquake.FieldID, field.TypeInt))
	_spec.From = feq.sql
	if unique := feq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if feq.path != nil {
		_spec.Unique = true
	}
	if fields := feq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ftypeearthquake.FieldID)
		for i := range fields {
			if fields[i] != ftypeearthquake.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if feq.withEarthquake != nil {
			_spec.Node.AddColumnOnce(ftypeearthquake.FieldEqID)
		}
		if feq.withFeatureType != nil {
			_spec.Node.AddColumnOnce(ftypeearthquake.FieldFtID)
		}
	}
	if ps := feq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := feq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := feq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := feq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (feq *FtypeEarthquakeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(feq.driver.Dialect())
	t1 := builder.Table(ftypeearthquake.Table)
	columns := feq.ctx.Fields
	if len(columns) == 0 {
		columns = ftypeearthquake.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if feq.sql != nil {
		selector = feq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if feq.ctx.Unique != nil && *feq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range feq.predicates {
		p(selector)
	}
	for _, p := range feq.order {
		p(selector)
	}
	if offset := feq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := feq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FtypeEarthquakeGroupBy is the group-by builder for FtypeEarthquake entities.
type FtypeEarthquakeGroupBy struct {
	selector
	build *FtypeEarthquakeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fegb *FtypeEarthquakeGroupBy) Aggregate(fns ...AggregateFunc) *FtypeEarthquakeGroupBy {
	fegb.fns = append(fegb.fns, fns...)
	return fegb
}

// Scan applies the selector query and scans the result into the given value.
func (fegb *FtypeEarthquakeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fegb.build.ctx, "GroupBy")
	if err := fegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FtypeEarthquakeQuery, *FtypeEarthquakeGroupBy](ctx, fegb.build, fegb, fegb.build.inters, v)
}

func (fegb *FtypeEarthquakeGroupBy) sqlScan(ctx context.Context, root *FtypeEarthquakeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fegb.fns))
	for _, fn := range fegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fegb.flds)+len(fegb.fns))
		for _, f := range *fegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FtypeEarthquakeSelect is the builder for selecting fields of FtypeEarthquake entities.
type FtypeEarthquakeSelect struct {
	*FtypeEarthquakeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fes *FtypeEarthquakeSelect) Aggregate(fns ...AggregateFunc) *FtypeEarthquakeSelect {
	fes.fns = append(fes.fns, fns...)
	return fes
}

// Scan applies the selector query and scans the result into the given value.
func (fes *FtypeEarthquakeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fes.ctx, "Select")
	if err := fes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FtypeEarthquakeQuery, *FtypeEarthquakeSelect](ctx, fes.FtypeEarthquakeQuery, fes, fes.inters, v)
}

func (fes *FtypeEarthquakeSelect) sqlScan(ctx context.Context, root *FtypeEarthquakeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fes.fns))
	for _, fn := range fes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
