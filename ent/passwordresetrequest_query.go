// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.com/hedwig-phan/assignment-3/ent/passwordresetrequest"
	"gitlab.com/hedwig-phan/assignment-3/ent/predicate"
	"gitlab.com/hedwig-phan/assignment-3/ent/user"
)

// PasswordResetRequestQuery is the builder for querying PasswordResetRequest entities.
type PasswordResetRequestQuery struct {
	config
	ctx        *QueryContext
	order      []passwordresetrequest.OrderOption
	inters     []Interceptor
	predicates []predicate.PasswordResetRequest
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PasswordResetRequestQuery builder.
func (prrq *PasswordResetRequestQuery) Where(ps ...predicate.PasswordResetRequest) *PasswordResetRequestQuery {
	prrq.predicates = append(prrq.predicates, ps...)
	return prrq
}

// Limit the number of records to be returned by this query.
func (prrq *PasswordResetRequestQuery) Limit(limit int) *PasswordResetRequestQuery {
	prrq.ctx.Limit = &limit
	return prrq
}

// Offset to start from.
func (prrq *PasswordResetRequestQuery) Offset(offset int) *PasswordResetRequestQuery {
	prrq.ctx.Offset = &offset
	return prrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prrq *PasswordResetRequestQuery) Unique(unique bool) *PasswordResetRequestQuery {
	prrq.ctx.Unique = &unique
	return prrq
}

// Order specifies how the records should be ordered.
func (prrq *PasswordResetRequestQuery) Order(o ...passwordresetrequest.OrderOption) *PasswordResetRequestQuery {
	prrq.order = append(prrq.order, o...)
	return prrq
}

// QueryUser chains the current query on the "user" edge.
func (prrq *PasswordResetRequestQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: prrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := prrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := prrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(passwordresetrequest.Table, passwordresetrequest.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, passwordresetrequest.UserTable, passwordresetrequest.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(prrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PasswordResetRequest entity from the query.
// Returns a *NotFoundError when no PasswordResetRequest was found.
func (prrq *PasswordResetRequestQuery) First(ctx context.Context) (*PasswordResetRequest, error) {
	nodes, err := prrq.Limit(1).All(setContextOp(ctx, prrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{passwordresetrequest.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) FirstX(ctx context.Context) *PasswordResetRequest {
	node, err := prrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PasswordResetRequest ID from the query.
// Returns a *NotFoundError when no PasswordResetRequest ID was found.
func (prrq *PasswordResetRequestQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prrq.Limit(1).IDs(setContextOp(ctx, prrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{passwordresetrequest.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) FirstIDX(ctx context.Context) int {
	id, err := prrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PasswordResetRequest entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PasswordResetRequest entity is found.
// Returns a *NotFoundError when no PasswordResetRequest entities are found.
func (prrq *PasswordResetRequestQuery) Only(ctx context.Context) (*PasswordResetRequest, error) {
	nodes, err := prrq.Limit(2).All(setContextOp(ctx, prrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{passwordresetrequest.Label}
	default:
		return nil, &NotSingularError{passwordresetrequest.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) OnlyX(ctx context.Context) *PasswordResetRequest {
	node, err := prrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PasswordResetRequest ID in the query.
// Returns a *NotSingularError when more than one PasswordResetRequest ID is found.
// Returns a *NotFoundError when no entities are found.
func (prrq *PasswordResetRequestQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prrq.Limit(2).IDs(setContextOp(ctx, prrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{passwordresetrequest.Label}
	default:
		err = &NotSingularError{passwordresetrequest.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) OnlyIDX(ctx context.Context) int {
	id, err := prrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PasswordResetRequests.
func (prrq *PasswordResetRequestQuery) All(ctx context.Context) ([]*PasswordResetRequest, error) {
	ctx = setContextOp(ctx, prrq.ctx, "All")
	if err := prrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PasswordResetRequest, *PasswordResetRequestQuery]()
	return withInterceptors[[]*PasswordResetRequest](ctx, prrq, qr, prrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) AllX(ctx context.Context) []*PasswordResetRequest {
	nodes, err := prrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PasswordResetRequest IDs.
func (prrq *PasswordResetRequestQuery) IDs(ctx context.Context) (ids []int, err error) {
	if prrq.ctx.Unique == nil && prrq.path != nil {
		prrq.Unique(true)
	}
	ctx = setContextOp(ctx, prrq.ctx, "IDs")
	if err = prrq.Select(passwordresetrequest.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) IDsX(ctx context.Context) []int {
	ids, err := prrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prrq *PasswordResetRequestQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, prrq.ctx, "Count")
	if err := prrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, prrq, querierCount[*PasswordResetRequestQuery](), prrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) CountX(ctx context.Context) int {
	count, err := prrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prrq *PasswordResetRequestQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, prrq.ctx, "Exist")
	switch _, err := prrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (prrq *PasswordResetRequestQuery) ExistX(ctx context.Context) bool {
	exist, err := prrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PasswordResetRequestQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prrq *PasswordResetRequestQuery) Clone() *PasswordResetRequestQuery {
	if prrq == nil {
		return nil
	}
	return &PasswordResetRequestQuery{
		config:     prrq.config,
		ctx:        prrq.ctx.Clone(),
		order:      append([]passwordresetrequest.OrderOption{}, prrq.order...),
		inters:     append([]Interceptor{}, prrq.inters...),
		predicates: append([]predicate.PasswordResetRequest{}, prrq.predicates...),
		withUser:   prrq.withUser.Clone(),
		// clone intermediate query.
		sql:  prrq.sql.Clone(),
		path: prrq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (prrq *PasswordResetRequestQuery) WithUser(opts ...func(*UserQuery)) *PasswordResetRequestQuery {
	query := (&UserClient{config: prrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	prrq.withUser = query
	return prrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PasswordResetRequest.Query().
//		GroupBy(passwordresetrequest.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (prrq *PasswordResetRequestQuery) GroupBy(field string, fields ...string) *PasswordResetRequestGroupBy {
	prrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PasswordResetRequestGroupBy{build: prrq}
	grbuild.flds = &prrq.ctx.Fields
	grbuild.label = passwordresetrequest.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.PasswordResetRequest.Query().
//		Select(passwordresetrequest.FieldUserID).
//		Scan(ctx, &v)
func (prrq *PasswordResetRequestQuery) Select(fields ...string) *PasswordResetRequestSelect {
	prrq.ctx.Fields = append(prrq.ctx.Fields, fields...)
	sbuild := &PasswordResetRequestSelect{PasswordResetRequestQuery: prrq}
	sbuild.label = passwordresetrequest.Label
	sbuild.flds, sbuild.scan = &prrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PasswordResetRequestSelect configured with the given aggregations.
func (prrq *PasswordResetRequestQuery) Aggregate(fns ...AggregateFunc) *PasswordResetRequestSelect {
	return prrq.Select().Aggregate(fns...)
}

func (prrq *PasswordResetRequestQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range prrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, prrq); err != nil {
				return err
			}
		}
	}
	for _, f := range prrq.ctx.Fields {
		if !passwordresetrequest.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if prrq.path != nil {
		prev, err := prrq.path(ctx)
		if err != nil {
			return err
		}
		prrq.sql = prev
	}
	return nil
}

func (prrq *PasswordResetRequestQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PasswordResetRequest, error) {
	var (
		nodes       = []*PasswordResetRequest{}
		_spec       = prrq.querySpec()
		loadedTypes = [1]bool{
			prrq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PasswordResetRequest).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PasswordResetRequest{config: prrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := prrq.withUser; query != nil {
		if err := prrq.loadUser(ctx, query, nodes, nil,
			func(n *PasswordResetRequest, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (prrq *PasswordResetRequestQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*PasswordResetRequest, init func(*PasswordResetRequest), assign func(*PasswordResetRequest, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PasswordResetRequest)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (prrq *PasswordResetRequestQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prrq.querySpec()
	_spec.Node.Columns = prrq.ctx.Fields
	if len(prrq.ctx.Fields) > 0 {
		_spec.Unique = prrq.ctx.Unique != nil && *prrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, prrq.driver, _spec)
}

func (prrq *PasswordResetRequestQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(passwordresetrequest.Table, passwordresetrequest.Columns, sqlgraph.NewFieldSpec(passwordresetrequest.FieldID, field.TypeInt))
	_spec.From = prrq.sql
	if unique := prrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if prrq.path != nil {
		_spec.Unique = true
	}
	if fields := prrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, passwordresetrequest.FieldID)
		for i := range fields {
			if fields[i] != passwordresetrequest.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if prrq.withUser != nil {
			_spec.Node.AddColumnOnce(passwordresetrequest.FieldUserID)
		}
	}
	if ps := prrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prrq *PasswordResetRequestQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prrq.driver.Dialect())
	t1 := builder.Table(passwordresetrequest.Table)
	columns := prrq.ctx.Fields
	if len(columns) == 0 {
		columns = passwordresetrequest.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prrq.sql != nil {
		selector = prrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prrq.ctx.Unique != nil && *prrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range prrq.predicates {
		p(selector)
	}
	for _, p := range prrq.order {
		p(selector)
	}
	if offset := prrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PasswordResetRequestGroupBy is the group-by builder for PasswordResetRequest entities.
type PasswordResetRequestGroupBy struct {
	selector
	build *PasswordResetRequestQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prrgb *PasswordResetRequestGroupBy) Aggregate(fns ...AggregateFunc) *PasswordResetRequestGroupBy {
	prrgb.fns = append(prrgb.fns, fns...)
	return prrgb
}

// Scan applies the selector query and scans the result into the given value.
func (prrgb *PasswordResetRequestGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prrgb.build.ctx, "GroupBy")
	if err := prrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PasswordResetRequestQuery, *PasswordResetRequestGroupBy](ctx, prrgb.build, prrgb, prrgb.build.inters, v)
}

func (prrgb *PasswordResetRequestGroupBy) sqlScan(ctx context.Context, root *PasswordResetRequestQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(prrgb.fns))
	for _, fn := range prrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*prrgb.flds)+len(prrgb.fns))
		for _, f := range *prrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*prrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PasswordResetRequestSelect is the builder for selecting fields of PasswordResetRequest entities.
type PasswordResetRequestSelect struct {
	*PasswordResetRequestQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (prrs *PasswordResetRequestSelect) Aggregate(fns ...AggregateFunc) *PasswordResetRequestSelect {
	prrs.fns = append(prrs.fns, fns...)
	return prrs
}

// Scan applies the selector query and scans the result into the given value.
func (prrs *PasswordResetRequestSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prrs.ctx, "Select")
	if err := prrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PasswordResetRequestQuery, *PasswordResetRequestSelect](ctx, prrs.PasswordResetRequestQuery, prrs, prrs.inters, v)
}

func (prrs *PasswordResetRequestSelect) sqlScan(ctx context.Context, root *PasswordResetRequestQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(prrs.fns))
	for _, fn := range prrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*prrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
